# LLM Configuration
LLM_PROVIDER=openai  # or vllm
MODEL=gpt-4o-mini

# OpenAI Configuration (required for embeddings, optional for LLM if using vLLM)
OPENAI_API_KEY=your_openai_api_key_here

# vLLM Configuration (if using vLLM provider)
VLLM_BASE_URL=http://localhost:8000/v1
VLLM_API_KEY=sk-local
VLLM_MODEL=your_vllm_model_name

# Embeddings Configuration
EMBED_MODEL=text-embedding-3-small

# Enhanced Memory System Examples:
# Run with default user and thread:
#   python scripts/chat.py

# Run with specific user and thread:
#   python scripts/chat.py --user alice --thread project-meeting

# Configure memory limits:
#   python scripts/chat.py --max-messages 50 --max-threads 5

# Available commands in chat:
#   /threads        - List your threads
#   /users          - List all users  
#   /switch <id>    - Switch to thread
#   /switch user <user> [thread] - Switch user/thread
#   /new [name]     - Create new thread
#   /title <name>   - Set thread title
#   /tag <name>     - Add thread tag
#   /info           - Show session info
#   /cleanup        - Clean old threads
#   /reset          - Clear current thread
#   /exit           - Save and exit

# PDF Support Installation:
# For best bank handbook support (recommended):
#   poetry install --extras pdf-best
# 
# For basic PDF support:
#   poetry install --extras pdf-basic
#
# For all PDF options:
#   poetry install --extras all-pdf
#
# Test PDF support:
#   python test_pdf_support.py
